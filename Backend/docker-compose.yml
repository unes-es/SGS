# docker-compose.yml
services:
  postgres:
    image: postgres:16-alpine
    container_name: sgs_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: sgs_user
      POSTGRES_PASSWORD: sgs_pass
      POSTGRES_DB: sgs_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: sgs_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@sgs.ma
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  postgres_data: